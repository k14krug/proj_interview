# K2 Daily Brief - example environment config
# Copy to .env and adjust for your machine.

# Application environment
K2_ENV=development

# Flask dev server port
K2_WEB_PORT=5000

# Used by Flask for sessions (dev-only example; change for production)
K2_SECRET_KEY=dev-change-me

# Worker process toggle (web should usually set false)
WORKER_ENABLED=false
